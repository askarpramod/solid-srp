<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Target Name="RunTestsAfterBuild" AfterTargets="Build">
    <Exec Command="dotnet test --no-build" IgnoreExitCode="false"/>
  </Target>
</Project>
